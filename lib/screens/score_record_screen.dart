import 'package:flutter/material.dart';

class ScoreRecordScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('スコア記録'),
      ),
      body: Center(
        child: Text('スコア記録画面'), // TODO: スコア記録画面のUIを実装
      ),
    );
  }
}
